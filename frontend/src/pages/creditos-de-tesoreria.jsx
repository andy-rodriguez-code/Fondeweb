import CreditoPage from './CreditoPage.jsx'

// creditos-de-tesoreria — página de la línea de crédito (clon creditos-de-tesoreria.html).
// Composición idéntica en las 9 páginas .credito (ver CreditoPage.jsx);
// el contenido verbatim vive en creditos.js (P2, solo lectura).

export default function CreditosDeTesoreria() {
  return <CreditoPage slug="creditos-de-tesoreria" />
}
