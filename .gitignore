node_modules/
dist/
.env
.env.*
# La plantilla sí se versiona: no tiene valores, solo los nombres.
!.env.example

# Credenciales del backend. Van acá desde antes de que exista la carpeta
# backend/: una contraseña que entra al historial de Git no se saca borrando el
# archivo, hay que reescribir el historial. Más barato prevenirlo.
config.php
**/config.php
!**/config.example.php
*credentials*.json
*service-account*.json
google-credentials.json
*.pem
*.key

# Estado en tiempo de ejecución del backend: registro con IPs, contador del
# límite por IP y las firmas en PNG de quien se inscribe. Son datos personales
# y datos de operación, no código. Nunca al repositorio.
backend/estado/
estado/
RECON/
*.artifact.json
.od-skills/
.atl/
.file-versions/
openspec/
archivos-listos/
